# Private Health Data Exports (Do not commit)
20250221_SamsungHealth/
data/samsung_export/

# Local Python Caching and Outputs
output/
local_cache.pkl
*.pkl

# Python virtual environment & runtime caches
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Rust build output
wellfare-core/target/

# OS metadata
.DS_Store
Thumbs.db

# Large 3rd-party 3D reference asset libraries (very large, not needed for core app)
/ccf-3d-reference-object-library-main/

# Generated Stlite PWA bundle (very large, built artifact - do not commit the *old* huge version)
# The current lean version (~8MB) is committed so GitHub Pages can serve it.
# docs/app.html
docs/app.tmp.html
