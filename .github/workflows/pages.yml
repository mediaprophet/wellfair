name: Deploy GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Install wasm-pack
        run: |
          curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build wellfare-core WASM
        run: |
          mkdir -p docs
          cd wellfare-core
          wasm-pack build --release --target web --out-dir ../docs/pkg

      - name: Copy web frontend to docs
        run: |
          cp -rn web/* docs/ || true

      - name: Copy repository docs assets into docs site
        run: |
          cp README.md docs/README.md || true
          cp COPYRIGHT.md docs/COPYRIGHT.md || true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/deploy-pages@v1
